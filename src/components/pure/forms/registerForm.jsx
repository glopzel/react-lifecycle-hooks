import React from 'react'

const RegisterForm = () => {
    const initialData = [
        {
            user: '',
            email: ''
        }
    ]

    const [data, setData] = usestate(initialData)

    return (
        <div></div>
    )
}